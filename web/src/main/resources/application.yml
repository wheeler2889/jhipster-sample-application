spring:
  datasource:
    url: jdbc:h2:mem:testdb
  jackson:
    date-format: com.mycompany.myapp.configuration.RFC3339DateFormat
    serialization:
      write-dates-as-timestamps: false
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true

server.port: 8080
